fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9          # multipart file upload for POST /api/citations/verify (frontend)
pydantic>=2.6
pydantic-settings>=2.2
pypdf>=4.0
langchain>=0.3
langchain-openai>=0.2
langchain-google-vertexai>=2.0    # provider "vertex" (needs a GCP project)
langchain-google-genai>=2.0       # provider "gemini" (Developer API, API key only)
python-dotenv>=1.0

# Citation resolution / semantic index (fallback resolver)
numpy>=1.26
fastembed>=0.3
rapidfuzz>=3.0          # required by app/services/citelib.py (name matching)

# PDF page rendering — used by the source-metadata builder to rasterise the first
# pages of each judgment for vision OCR (app/services/source_metadata_builder.py),
# and by the OCR fallback (app/services/pdf_ocr.py).
pymupdf>=1.24
tqdm>=4.66          # progress bars for the source builders

# Optional — local OCR fallback for scanned source PDFs (app/services/pdf_ocr.py).
# Heavy; install only if you use the local OCR path instead of vision OCR:
#   rapidocr-onnxruntime>=1.3
